import { Routes } from '@angular/router';

// Home is the only route wired up so far (Header, Category Nav, Hero).
// Add About / Products / Wholesale & Trade / Clients / Contact routes here
// as those pages are built.
export const routes: Routes = [];
